<% if @current_review %>
  <h4> Your review </h4>
  <p><%= @current_review %></p>
  <%= link_to "Edit", edit_review_path(@connection, @meeting) %>
  <%= link_to "Delete", review_path(@connection, @meeting), method: :delete %>
<% else %>
  <h2> Make a review </h2>
  <%= render "form_review" %>
<% end %>


<% if @other_review %>
  <h4><%= "#{@other_meeter.first_name} #{@other_meeter.last_name} 's Review" %></h4>
  <p style="font-size: 14px"><%= @other_review %></p>
<% else %>
  <h4><%= "#{@other_meeter.first_name} #{@other_meeter.last_name} n'a pas encore fait de review" %></h4>
<% end %>

