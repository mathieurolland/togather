<p><%= link_to "Go back to the list", places_path %></p>

<div class="container">
    <p><h2><%= @place.name %></h2></p>
    <p><strong>Address: </strong><%= @place.address %></p>
    <p><strong>Description: </strong><%= @place.description %></p>
    <p><strong>Phone number: </strong><%= @place.phone_number %></p>
    <p><strong>Type: </strong> <%= @place.type_partner %></p>
    <p><strong>Pax: </strong> <%= @place.pax %></p>
    </br>
</div>

<p>Menu à montrer uniquement aux partners(pas de new!)</p>
<% if @place.user == current_user %>
  <p><%= link_to "Edit", edit_place_path %></p>
  <p><%= link_to "Delete", place_path(@place), method: :delete, data: { confirm: "Are you sure you want to delete this product?" } %></p>
<% end %>

